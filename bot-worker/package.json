{
  "name": "eql-product-search-bot-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Discord slash-command search endpoint for the EQL monitor state",
  "scripts": {
    "check": "node --check src/index.js && node --check src/search.js && node --check scripts/register-command.js",
    "test": "node --test tests/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register": "node scripts/register-command.js"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.33.0"
  },
  "engines": {
    "node": ">=22"
  }
}
